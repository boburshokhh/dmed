# 🚀 Инструкция по деплою фронтенда

## Подготовка к деплою

### 1. Настройка переменных окружения

#### Для Netlify:

1. Перейдите в **Netlify Dashboard**
2. Выберите ваш проект
3. Перейдите в **Site settings** > **Environment variables**
4. Добавьте переменную:

```
NEXT_PUBLIC_API_URL = https://your-backend-url.com
```

**Важно:** Замените `https://your-backend-url.com` на реальный URL вашего бэкенда.

#### Для локальной разработки:

Создайте файл `.env.local` в папке `front/`:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

**⚠️ Внимание:** Файл `.env.local` не должен попадать в git (уже добавлен в .gitignore)

### 2. Проверка конфигурации

#### Файлы конфигурации:

- ✅ `netlify.toml` - конфигурация для Netlify
- ✅ `next.config.js` - конфигурация Next.js
- ✅ `.env.example` - пример переменных окружения

### 3. Сборка проекта

#### Локальная сборка (для тестирования):

```bash
cd front
npm install
npm run build
npm start
```

#### Деплой на Netlify:

1. **Через Git:**
   - Закоммитьте изменения
   - Запушьте в репозиторий
   - Netlify автоматически соберет и задеплоит проект

2. **Через Netlify CLI:**
   ```bash
   npm install -g netlify-cli
   netlify login
   netlify deploy --prod
   ```

3. **Через Drag & Drop:**
   - Соберите проект: `npm run build`
   - Перетащите папку `.next` в Netlify Dashboard

## Структура переменных окружения

### NEXT_PUBLIC_API_URL

**Описание:** URL бэкенда для API запросов

**Примеры:**
- Локальная разработка: `http://localhost:5000`
- Продакшн: `https://api.dmed.uz` или `https://your-backend.herokuapp.com`

**Где используется:**
- `app/page.js` - проверка PIN-кода
- `app/access/[uuid]/page.js` - загрузка документа и проверка PIN
- `app/el-docs/[uuid]/page.js` - загрузка данных документа

## Проверка после деплоя

### 1. Проверьте переменные окружения

В Netlify Dashboard убедитесь что:
- ✅ `NEXT_PUBLIC_API_URL` установлена
- ✅ Значение правильное (без trailing slash)
- ✅ Переменная доступна для Production

### 2. Проверьте работу API

Откройте консоль браузера (F12) и проверьте:
- Нет ли ошибок CORS
- Загружаются ли данные с бэкенда
- Работает ли проверка PIN-кода

### 3. Тестовые сценарии

1. **Главная страница:**
   - Откройте `/`
   - Введите PIN-код
   - Должно перенаправить на `/el-docs/{uuid}`

2. **Страница доступа:**
   - Откройте `/access/{uuid}`
   - Введите PIN-код
   - Должно перенаправить на `/el-docs/{uuid}`

3. **Просмотр документа:**
   - Откройте `/el-docs/{uuid}`
   - Должен загрузиться PDF
   - Кнопка печати должна работать

## Troubleshooting

### Проблема: CORS ошибки

**Решение:**
- Убедитесь что бэкенд настроен для работы с вашим доменом
- Проверьте что `NEXT_PUBLIC_API_URL` правильный
- Проверьте CORS настройки на бэкенде

### Проблема: API запросы идут на localhost

**Решение:**
- Проверьте что переменная `NEXT_PUBLIC_API_URL` установлена в Netlify
- Пересоберите проект после изменения переменных
- Очистите кэш браузера

### Проблема: PDF не загружается

**Решение:**
- Проверьте что `pdf_url_by_uuid` возвращается из API
- Проверьте CORS для `/download-by-uuid/*` на бэкенде
- Проверьте что PDF файл доступен по URL

## Оптимизация для продакшна

### 1. Минификация и оптимизация

Next.js автоматически:
- ✅ Минифицирует JavaScript и CSS
- ✅ Оптимизирует изображения
- ✅ Использует code splitting

### 2. Кэширование

Netlify автоматически кэширует:
- Статические файлы
- Изображения
- CSS и JS файлы

### 3. CDN

Netlify использует глобальный CDN для:
- Быстрой загрузки статических ресурсов
- Оптимизации доставки контента

## Мониторинг

### Netlify Analytics

Включите Netlify Analytics для отслеживания:
- Количество посещений
- Производительность загрузки
- Ошибки

### Логи

Проверяйте логи в Netlify Dashboard:
- **Deploy logs** - ошибки сборки
- **Function logs** - ошибки серверных функций (если используются)

## Безопасность

### ✅ Что уже настроено:

- Переменные окружения не попадают в git
- `.env.local` в `.gitignore`
- HTTPS включен по умолчанию на Netlify

### ⚠️ Рекомендации:

- Не храните секреты в `NEXT_PUBLIC_*` переменных
- Используйте HTTPS для всех API запросов
- Регулярно обновляйте зависимости

---

✅ **Готово к деплою!**

