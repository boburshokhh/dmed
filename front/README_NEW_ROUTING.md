# 🎉 Новый роутинг для просмотра документов

## Что изменилось?

### ✅ Старый подход (не используется)
```
После ввода PIN → /document/№%2001ВШ%20081125568?data={...огромный JSON...}
```
❌ Данные в URL  
❌ Длинный URL  
❌ Небезопасно  

### ✅ Новый подход (текущий)
```
После ввода PIN → /el-docs/31ef792c-5e76-4bc3-b32c-de98a97e5999
```
✅ Чистый URL  
✅ Данные загружаются через API  
✅ Безопасно  

## Как это работает

### 1️⃣ Проверка документа
Пользователь вводит PIN-код на странице:
- `/` (главная страница)
- `/access/{uuid}` (страница доступа)

### 2️⃣ Перенаправление
После успешной проверки:
```javascript
// Перенаправление на новый роут
window.location.href = `/el-docs/${uuid}`
```

### 3️⃣ Загрузка данных
Страница `/el-docs/{uuid}` автоматически:
1. Загружает данные документа через API: `GET /api/access/{uuid}`
2. Получает URL PDF файла
3. Отображает PDF в полноэкранном режиме
4. Показывает кнопку печати

## Структура файлов

```
front/
├── app/
│   ├── page.js                    # Главная (обновлена)
│   ├── access/
│   │   └── [uuid]/
│   │       └── page.js            # Страница доступа (обновлена)
│   ├── el-docs/                   # ✨ НОВЫЙ роут
│   │   └── [uuid]/
│   │       └── page.js            # Страница PDF документа
│   └── document/
│       └── [docNumber]/
│           └── page.js            # Старая страница (не используется)
```

## Примеры URL

### Главная страница
```
http://localhost:3000/
```

### Страница доступа по UUID
```
http://localhost:3000/access/31ef792c-5e76-4bc3-b32c-de98a97e5999
```

### Страница просмотра документа (новая)
```
http://localhost:3000/el-docs/31ef792c-5e76-4bc3-b32c-de98a97e5999
```

## Безопасность

### ✅ Что защищено:
- UUID в URL не содержит персональных данных
- Данные документа загружаются только после проверки прав доступа
- Нет утечки информации через URL

### ✅ Как проверяется доступ:
1. Пользователь вводит PIN-код
2. Сервер проверяет соответствие PIN и UUID
3. Только после проверки выдаются данные документа

## API Endpoints

### Получение документа
```
GET /api/access/{uuid}
```

Ответ:
```json
{
  "success": true,
  "document": {
    "uuid": "...",
    "doc_number": "...",
    "patient_name": "...",
    "pdf_url": "http://localhost:5000/download/70",
    "pdf_url_by_uuid": "http://localhost:5000/download-by-uuid/{uuid}",
    ...
  }
}
```

### Проверка PIN-кода
```
POST /api/access/{uuid}/verify-pin
Body: { "pin_code": "1234" }
```

## Возможности страницы просмотра

### 📄 Отображение PDF
- Полноэкранный просмотр
- Адаптивный дизайн (телефоны, планшеты, десктоп)
- Плавная прокрутка между страницами
- Без лишних границ и элементов

### 🖨️ Печать
- Кнопка печати внизу справа
- Открывает диалог печати браузера
- Работает на всех устройствах

### 📱 Мобильная поддержка
- Адаптивная ширина PDF
- Оптимизированная загрузка
- Плавная анимация

## Запуск приложения

```bash
# Перейти в папку front
cd front

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

Приложение будет доступно: **http://localhost:3000**

## Что нужно установить на сервере

### Backend (Python)
```bash
pip install flask-cors
```

### Frontend (Node.js)
```bash
npm install
```

Все зависимости уже указаны в `package.json`.

---

✅ **Всё готово к использованию!**

