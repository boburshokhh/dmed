# 🚀 Быстрый старт для деплоя

## Шаг 1: Настройка переменных окружения в Netlify

1. Откройте **Netlify Dashboard** → Ваш проект
2. Перейдите: **Site settings** → **Environment variables**
3. Добавьте новую переменную:

```
Key: NEXT_PUBLIC_API_URL
Value: https://your-backend-url.com
```

**⚠️ Важно:** Замените `https://your-backend-url.com` на реальный URL вашего продакшн бэкенда!

## Шаг 2: Деплой

### Вариант A: Автоматический деплой через Git

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Prepare for production"
   git push
   ```

2. Netlify автоматически соберет и задеплоит проект

### Вариант B: Ручной деплой

1. Соберите проект:
   ```bash
   cd front
   npm install
   npm run build
   ```

2. Загрузите папку `.next` в Netlify через Drag & Drop

## Шаг 3: Проверка

После деплоя проверьте:

- ✅ Сайт открывается
- ✅ Нет ошибок в консоли браузера (F12)
- ✅ Работает проверка PIN-кода
- ✅ Загружается PDF документ

## 📝 Где используется API URL

Переменная `NEXT_PUBLIC_API_URL` используется в:

1. **Главная страница** (`app/page.js`) - проверка PIN-кода
2. **Страница доступа** (`app/access/[uuid]/page.js`) - загрузка документа
3. **Просмотр документа** (`app/el-docs/[uuid]/page.js`) - загрузка PDF

## 🔧 Локальная разработка

Для локальной разработки создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

## ❓ Проблемы?

См. подробную инструкцию в файле `DEPLOY.md`

---

✅ **Готово!** После настройки переменных окружения проект готов к деплою.

