# Сдвиг QR-кода на левую сторону

## ✅ Изменения

### Выравнивание QR-кода изменено с RIGHT на LEFT

**Было:**
- QR-код выровнен по **правому краю** ячейки
- Отрицательный right_indent (-360 twips) для сдвига вправо

**Стало:**
- QR-код выровнен по **левому краю** ячейки
- Отрицательный right_indent убран

---

## 🔧 Технические изменения

### 1. Выравнивание QR-кода

**Было:**
```python
para_qr.alignment = WD_ALIGN_PARAGRAPH.RIGHT  # По правому краю
# Устанавливаем отрицательный right_indent через XML для сдвига вправо
ind_qr.set(qn('w:right'), '-360')  # Отрицательный отступ ~24px
```

**Стало:**
```python
para_qr.alignment = WD_ALIGN_PARAGRAPH.LEFT  # По левому краю
para_qr_format.left_indent = Pt(0)  # Нет отступа слева - прижимаем к левому краю
para_qr_format.right_indent = Pt(0)  # Отрицательный right_indent убран
```

### 2. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До (QR-код справа):
```
┌─────────────────────────────────────┐
│          2653              [QR]    │  ← QR-код справа
│                                     │
└─────────────────────────────────────┘
Выравнивание: PIN=RIGHT, QR=RIGHT
```

### После (QR-код слева):
```
┌─────────────────────────────────────┐
│          2653 [QR]                 │  ← QR-код слева, близко к PIN
│                                     │
└─────────────────────────────────────┘
Выравнивание: PIN=RIGHT, QR=LEFT
```

---

## 🎯 Результат

После изменений:

✅ **QR-код выровнен по левому краю** ячейки  
✅ **QR-код максимально близко к PIN-коду** (нет отступов между ними)  
✅ **PIN-код остается справа** (выравнивание не изменилось)  
✅ **Оба элемента вместе** (близко друг к другу)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Текущие параметры

### Выравнивание:
- **PIN-код**: RIGHT (по правому краю ячейки)
- **QR-код**: LEFT (по левому краю ячейки)

### Отступы ячеек (tcMar):
- **PIN**: Top=0, Bottom=0, Left=0, Right=0
- **QR**: Top=0, Bottom=0, Left=0, Right=0

### Отступы параграфов:
- **PIN**: space_before=0, space_after=0, left_indent=0, right_indent=0
- **QR**: space_before=0, space_after=0, left_indent=0, right_indent=0

### Ширина ячеек:
- **PIN**: 2.0 см
- **QR**: 5.0 см

### Cell spacing таблицы:
- **0** (нет отступов между ячейками)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что QR-код выровнен по левому краю и близко к PIN-коду

---

## ⚠️ Важные замечания

1. **QR-код теперь слева** - выровнен по левому краю ячейки
2. **PIN-код остается справа** - выровнен по правому краю ячейки
3. **Оба элемента близко друг к другу** - нет отступов между ними благодаря нулевым margin и cell spacing

4. **Если нужно сдвинуть QR-код еще левее:**
   - Можно использовать отрицательный left_indent (но это может обрезать QR-код)
   - Или уменьшить ширину ячейки QR-кода

5. **Если нужно вернуть QR-код вправо:**
   - Измените `WD_ALIGN_PARAGRAPH.LEFT` обратно на `WD_ALIGN_PARAGRAPH.RIGHT`
   - И добавьте отрицательный right_indent через XML

---

## 📝 Резюме

**Изменение:** Выравнивание QR-кода с RIGHT на LEFT  
**Применено к:** Ячейка QR-кода в обеих таблицах  
**Результат:** QR-код выровнен по левому краю ячейки и близко к PIN-коду ✅

После применения изменений QR-код будет выровнен по левому краю ячейки и максимально близко к PIN-коду!

