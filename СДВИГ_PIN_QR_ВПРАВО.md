# Сдвиг PIN-кода и QR-кода немного вправо

## ✅ Изменения

### Добавлен небольшой left_indent для сдвига вправо

**Было:**
- PIN-код: `left_indent = Pt(0)` (без отступа)
- QR-код: `left_indent = Pt(0)` (без отступа)

**Стало:**
- PIN-код: `left_indent = Pt(10)` (~10px сдвиг вправо)
- QR-код: `left_indent = Pt(10)` (~10px сдвиг вправо)

---

## 🔧 Технические изменения

### 1. Left_indent для PIN-кода

**Было:**
```python
para_pin_format.left_indent = Pt(0)
para_pin_format.right_indent = Pt(0)
# Был отрицательный right_indent через XML
```

**Стало:**
```python
para_pin_format.left_indent = Pt(10)  # Сдвиг вправо на ~10px
para_pin_format.right_indent = Pt(0)
# Убран отрицательный right_indent
```

### 2. Left_indent для QR-кода

**Было:**
```python
para_qr_format.left_indent = Pt(0)  # Нет отступа слева
```

**Стало:**
```python
para_qr_format.left_indent = Pt(10)  # Сдвиг вправо на ~10px
```

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До (без отступа):
```
┌─────────────────────────────────────┐
│          2653 [QR]                 │  ← Прижато к краям
└─────────────────────────────────────┘
```

### После (с отступом ~10px):
```
┌─────────────────────────────────────┐
│           2653 [QR]                 │  ← Сдвинуто вправо на ~10px
└─────────────────────────────────────┘
```

---

## 🎯 Результат

После изменений:

✅ **PIN-код сдвинут вправо** на ~10px  
✅ **QR-код сдвинут вправо** на ~10px  
✅ **Оба элемента вместе** (одинаковый сдвиг)  
✅ **Близко друг к другу** (минимальное расстояние сохранено)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Технические детали

### Конвертация единиц:
- **10px** ≈ **7.5pt** ≈ **150 twips**
- Используется **Pt(10)** (~10px) для небольшого сдвига

### Механизм работы:
1. **Left_indent** сдвигает содержимое параграфа вправо от левого края
2. Для **PIN-кода** (выровненного по правому краю) это создает дополнительное пространство слева
3. Для **QR-кода** (выровненного по левому краю) это сдвигает его вправо
4. Оба элемента получают одинаковый сдвиг

---

## 📐 Текущие параметры

### Отступы параграфов:
- **PIN**: space_before=0, space_after=0, **left_indent=Pt(10)**, right_indent=0
- **QR**: space_before=0, space_after=0, **left_indent=Pt(10)**, right_indent=0

### Выравнивание:
- **PIN-код**: RIGHT (по правому краю) или LEFT (согласно параметру)
- **QR-код**: LEFT (по левому краю) или RIGHT (согласно параметру)

### Отступы ячеек (tcMar):
- Все отступы: 0 (не изменились)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код и QR-код сдвинуты немного вправо

---

## ⚠️ Важные замечания

1. **Left_indent работает для обоих элементов** - сдвигает содержимое вправо
2. Если нужно больше сдвига:
   - Увеличьте значение `Pt(10)` до `Pt(15)` или `Pt(20)` (больше сдвиг)
   - Или уменьшите до `Pt(5)` (меньше сдвиг)

3. **Если нужно меньше сдвига:**
   - Уменьшите значение `Pt(10)` до `Pt(5)` или `Pt(7)`

4. **Если результат не соответствует ожиданиям:**
   - Проверьте версию LibreOffice/Word
   - Убедитесь, что left_indent правильно применяется
   - Попробуйте изменить значение отступа

---

## 📝 Резюме

**Изменение:** Добавлен left_indent = Pt(10) для PIN-кода и QR-кода  
**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** Оба элемента сдвинуты вправо на ~10px ✅

После применения изменений PIN-код и QR-код будут сдвинуты немного вправо и останутся вместе!

