# Сдвиг PIN-кода и QR-кода правее от правого края ячейки

## ✅ Изменения

### Добавлен отрицательный right margin для сдвига правее

**Было:**
- Right margin ячейки: `0` (нулевой, прижато к правому краю)

**Стало:**
- Right margin ячейки: `-300 twips` (отрицательный, ~20px правее от края)

---

## 🔧 Технические изменения

### 1. Right margin ячейки PIN-кода

**Было:**
```python
right_margin_pin.set(qn('w:w'), '0')  # Нулевой отступ
```

**Стало:**
```python
right_margin_pin.set(qn('w:w'), '-300')  # Отрицательный отступ ~20px для сдвига правее
```

### 2. Right margin ячейки QR-кода

**Было:**
```python
right_margin_qr.set(qn('w:w'), '0')  # Нулевой отступ
```

**Стало:**
```python
right_margin_qr.set(qn('w:w'), '-300')  # Отрицательный отступ ~20px для сдвига правее
```

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До изменения (прижато к правому краю):
```
┌─────────────────────────────────────┐
│                           2653 [QR] │  ← Прижато к правому краю
│                                     │
└─────────────────────────────────────┘
```

### После изменения (сдвинуто правее на ~20px):
```
┌─────────────────────────────────────┐
│                        2653 [QR]   │  ← Сдвинуто правее на ~20px
│                                     │
└─────────────────────────────────────┘
```

---

## 🎯 Результат

После изменений:

✅ **PIN-код сдвинут правее** от правого края ячейки (~20px)  
✅ **QR-код сдвинут правее** от правого края ячейки (~20px)  
✅ **Оба элемента сдвинуты вместе** (одинаковый отступ)  
✅ **Близко друг к другу** (минимальное расстояние между ними сохранено)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Технические детали

### Конвертация единиц:
- **20px** ≈ **15pt** ≈ **300 twips**
- Используется **-300 twips** (точные 20px)

### Механизм работы:
- **Отрицательный right margin** ячейки уменьшает внутреннее пространство справа
- При выравнивании **RIGHT** это сдвигает содержимое правее от правого края ячейки
- Оба элемента (PIN и QR) получают одинаковый сдвиг, поэтому остаются вместе

---

## 📐 Параметры остались прежними

- **Ширина ячеек**: PIN=2.0 см, QR=3.8 см
- **Размер QR-кода**: 1.1 дюйма (~2.79 см)
- **Размер шрифта PIN-кода**: 20pt
- **Вертикальное выравнивание**: CENTER (не изменилось)
- **Горизонтальное выравнивание**: RIGHT (не изменилось)
- **Отступы**: 
  - Top, bottom, left: 0 (для PIN), 20 twips (для QR - минимальный для сближения)
  - Right: -300 twips (~20px) для обеих ячеек

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код и QR-код сдвинуты правее от правого края

---

## ⚠️ Важные замечания

1. **Отрицательные margin** могут не поддерживаться в некоторых версиях Word/LibreOffice
2. Если отрицательные margin не работают, можно использовать альтернативный подход:
   - Увеличить ширину ячеек на 20px
   - Или использовать left_indent для параграфов

3. **Если нужно больше сдвига:**
   - Увеличьте значение `-300` до `-360` или `-420` (больше сдвиг)
   - Или уменьшите до `-240` (меньше сдвиг)

4. **Если результат не соответствует ожиданиям:**
   - Проверьте версию LibreOffice/Word
   - Попробуйте изменить значение `-300` на другое
   - Или используйте положительный left_indent вместо отрицательного right margin

---

## 📝 Резюме

**Изменение:** Добавлен отрицательный right margin (-300 twips ≈ 20px)  
**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** Оба элемента сдвинуты правее от правого края ячейки на ~20px ✅

После применения изменений PIN-код и QR-код будут сдвинуты правее от правого края ячейки и останутся вместе!

