# Сближение PIN-кода и QR-кода

## ✅ Изменения

### Убран left_indent для QR-кода, чтобы сблизить его с PIN-кодом

**Было:**
- PIN-код: `left_indent = Pt(25)` (~25px сдвиг вправо)
- QR-код: `left_indent = Pt(25)` (~25px сдвиг вправо)
- **Результат:** Отступ между PIN и QR из-за left_indent QR-кода

**Стало:**
- PIN-код: `left_indent = Pt(25)` (~25px сдвиг вправо) - **не изменился**
- QR-код: `left_indent = Pt(0)` (нет отступа, прижат к левому краю ячейки)
- **Результат:** QR-код ближе к PIN-коду, так как нет left_indent

---

## 🔧 Технические изменения

### 1. Убран left_indent для QR-кода

**Было:**
```python
para_qr_format.left_indent = Pt(25)  # Сдвиг вправо на ~25px
```

**Стало:**
```python
para_qr_format.left_indent = Pt(0)  # Нет отступа - прижимаем к левому краю ячейки, ближе к PIN
```

### 2. PIN-код остался с left_indent

**Не изменилось:**
```python
para_pin_format.left_indent = Pt(25)  # Сдвиг вправо на ~25px
```

**Причина:** PIN-код остается сдвинутым вправо, а QR-код без left_indent будет ближе к нему.

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До (был отступ между PIN и QR):
```
┌─────────────────────────────────────┐
│           6758    [QR]              │  ← Отступ из-за left_indent QR-кода
└─────────────────────────────────────┘
PIN: left_indent = Pt(25)
QR: left_indent = Pt(25) ← создает отступ
```

### После (QR ближе к PIN):
```
┌─────────────────────────────────────┐
│           6758[QR]                  │  ← QR ближе к PIN, нет left_indent
└─────────────────────────────────────┘
PIN: left_indent = Pt(25) ← сдвинут вправо
QR: left_indent = Pt(0) ← прижат к левому краю, ближе к PIN
```

---

## 🎯 Результат

После изменений:

✅ **QR-код ближе к PIN-коду** - убран left_indent для QR-кода  
✅ **PIN-код остается сдвинутым вправо** - left_indent = Pt(25) сохранен  
✅ **Минимальное расстояние между элементами** - нет отступов между ними  
✅ **Расположение не изменилось** - оба элемента остались на своих местах, только сблизились

---

## 📐 Технические детали

### Отступы параграфов:
- **PIN**: space_before=0, space_after=0, **left_indent=Pt(25)**, right_indent=0
- **QR**: space_before=0, space_after=0, **left_indent=Pt(0)**, right_indent=0

### Отступы ячеек (tcMar):
- **PIN**: Top=0, Bottom=0, Left=0, **Right=0** (прижато к QR-коду)
- **QR**: Top=0, Bottom=0, **Left=0** (прижато к PIN-коду), Right=0

### Cell spacing таблицы:
- **0** (нет отступов между ячейками)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код и QR-код максимально близко друг к другу

---

## ⚠️ Важные замечания

1. **QR-код без left_indent** - прижат к левому краю ячейки, ближе к PIN-коду
2. **PIN-код с left_indent** - остается сдвинутым вправо на ~25px
3. **Оба элемента сближены** - нет отступов между ними

4. **Если нужно еще больше сблизить:**
   - Можно уменьшить left_indent для PIN-кода до `Pt(15)` или `Pt(10)`
   - Или убрать left_indent для PIN-кода полностью (`Pt(0)`)

5. **Если нужно вернуть отступ:**
   - Установите left_indent для QR-кода обратно в `Pt(25)`

---

## 📝 Резюме

**Изменение:** Убран left_indent для QR-кода (с Pt(25) до Pt(0))  
**Применено к:** Ячейка QR-кода в обеих таблицах  
**Результат:** QR-код ближе к PIN-коду, минимальное расстояние между ними ✅

После применения изменений PIN-код и QR-код будут максимально близко друг к другу без отступов!


