# Исправление обрезки QR-кода

## 🔍 Проблема

QR-код обрезается - видна только половина QR-кода.

**Причина:** Размер QR-кода (1.2 дюйма = ~3.05 см) был больше ширины ячейки (2.8 см).

---

## ✅ Исправления

### 1. Увеличена ширина ячейки для QR-кода

**Было:**
```python
inner_table.columns[1].width = Cm(2.8)  # Слишком узко
```

**Стало:**
```python
inner_table.columns[1].width = Cm(3.8)  # Достаточно для QR-кода
```

**Расчёт:**
- QR-код: 1.1 дюйма = ~2.79 см
- Ячейка: 3.8 см
- Запас: ~1 см (достаточно для отступов)

---

### 2. Уменьшен размер QR-кода

**Было:**
```python
qr_width_inches = 1.2  # ~3.05 см
```

**Стало:**
```python
qr_width_inches = 1.1  # ~2.79 см
```

**Причина:** Уменьшение размера обеспечивает запас в ячейке 3.8 см и предотвращает обрезку.

---

### 3. Минимальные отступы ячейки

**Уже было:**
- Отступы top, bottom, right: 0
- Отступ left: 20 twips (~2-3px) для сближения с PIN-кодом

**Результат:** QR-код не обрезается отступами.

---

## 📊 Итоговые параметры

### Ширина ячеек:
- **PIN-код**: 2.0 см
- **QR-код**: 3.8 см (увеличена с 2.8 см)
- **Общая ширина таблицы**: ~5.8 см

### Размер QR-кода:
- **Ширина**: 1.1 дюйма (~2.79 см)
- **В ячейке**: 3.8 см
- **Запас**: ~1 см (достаточно)

### Отступы ячейки QR-кода:
- **Top, Bottom, Right**: 0 (минимальные)
- **Left**: 20 twips (~2-3px) для сближения с PIN-кодом

---

## 🎯 Результат

После исправлений:

✅ **QR-код полностью виден** (не обрезается)  
✅ **Правильный размер** (1.1 дюйма)  
✅ **Помещается в ячейку** (3.8 см)  
✅ **Близко к PIN-коду** (минимальное расстояние)  
✅ **Выровнен по левому краю** ячейки  
✅ **По центру вертикально**

---

## 📐 Визуальное представление

### До исправления:
```
┌─────────────┬─────────────┐
│  2653       │  [QR-код    │  ← QR-код обрезан
│             │   обрезан]  │
└─────────────┴─────────────┘
```

### После исправления:
```
┌─────────────┬──────────────────┐
│  2653       │  [QR-код]        │  ← QR-код полностью виден
│             │                  │
└─────────────┴──────────────────┘
```

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что QR-код полностью виден

---

## ⚠️ Важные замечания

1. **Ширина ячейки критична** - если уменьшить до 2.8 см, QR-код снова будет обрезаться
2. **Размер QR-кода** - 1.1 дюйма оптимален для ячейки 3.8 см
3. **Отступы** - минимальные отступы предотвращают обрезку

---

## 📝 Резюме

**Проблема:** QR-код обрезается (видна только половина)  
**Причина:** Размер QR-кода (3.05 см) больше ширины ячейки (2.8 см)  
**Решение:** 
- Увеличена ширина ячейки до 3.8 см
- Уменьшен размер QR-кода до 1.1 дюйма (~2.79 см)
- Минимальные отступы

**Результат:** QR-код полностью виден, не обрезается ✅

После применения исправлений QR-код должен отображаться полностью!

