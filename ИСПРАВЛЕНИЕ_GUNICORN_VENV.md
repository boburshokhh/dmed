# Исправление проблемы с виртуальным окружением Gunicorn

## 🔍 Проблема найдена!

**Причина:** Gunicorn использует виртуальное окружение `.venv`, но библиотеки установлены в `venv`.

**Доказательства:**
- Процесс gunicorn: `/var/www/dmed/.venv/bin/python` 
- Тест работает: скрипт использует `venv` (где библиотеки есть)
- Ошибка: `ModuleNotFoundError` - в `.venv` библиотек нет

---

## ✅ Решение (выберите один вариант)

### Вариант 1: Установить библиотеки в .venv (РЕКОМЕНДУЕТСЯ)

Это самый простой способ - просто установить библиотеки в то окружение, которое использует gunicorn:

```bash
cd /var/www/dmed

# Активируем .venv (то, что использует gunicorn)
source .venv/bin/activate

# Устанавливаем библиотеки
pip install --upgrade pip
pip install -r requirements.txt

# Проверяем
python3 -c "import qrcode_styled; from PIL import Image; print('OK!')"

# Деактивируем
deactivate

# Перезапускаем сервис
sudo systemctl restart dmed
```

---

### Вариант 2: Использовать автоматический скрипт

```bash
cd /var/www/dmed
chmod +x fix_gunicorn_venv.sh
sudo bash fix_gunicorn_venv.sh
```

---

### Вариант 3: Изменить конфигурацию gunicorn на использование venv

Если хотите использовать `venv` вместо `.venv`:

1. Отредактируйте конфигурацию systemd:
```bash
sudo nano /etc/systemd/system/dmed.service
```

2. Найдите строку с `ExecStart` и измените путь с `.venv` на `venv`:
```ini
# Было:
ExecStart=/var/www/dmed/.venv/bin/gunicorn ...

# Должно быть:
ExecStart=/var/www/dmed/venv/bin/gunicorn ...
```

3. Перезагрузите конфигурацию:
```bash
sudo systemctl daemon-reload
sudo systemctl restart dmed
```

---

## 🔄 После исправления

### 1. Перезапустите сервис

```bash
sudo systemctl restart dmed
```

### 2. Проверьте логи

```bash
# Следите за логами в реальном времени
sudo journalctl -u dmed -f

# Или проверьте последние логи
sudo journalctl -u dmed -n 50 | grep -i qr
```

### 3. Ожидаемый результат

Должны появиться сообщения:
```
[QR_CODE] Начинаем генерацию QR-кода для URL: https://...
[QR_CODE] Используем библиотеку qrcode-styled
[QR_CODE] QR-код сгенерирован через qrcode-styled
[QR_CODE] Финальный QR-код: размер (864, 864), режим RGBA
[QR_PIN_LAYOUT] QR-код добавлен во вложенную таблицу
```

**НЕ должно быть:**
```
[ERROR] qrcode-styled не установлен: No module named 'qrcode_styled'
```

---

## 🧪 Проверка после исправления

Выполните тест:

```bash
cd /var/www/dmed
source .venv/bin/activate  # Используем .venv как gunicorn

python3 -c "
from utils import generate_qr_code
from PIL import Image

try:
    img = generate_qr_code('https://test.example.com')
    print('✓ QR-код генерируется успешно!')
    print(f'Размер: {img.size}, Режим: {img.mode}')
except Exception as e:
    print(f'✗ Ошибка: {e}')
    import traceback
    traceback.print_exc()
"
```

Если видите `✓ QR-код генерируется успешно!` - проблема решена!

---

## 📋 Резюме

**Проблема:** Gunicorn использует `.venv`, библиотеки в `venv`  
**Решение:** Установить библиотеки в `.venv` (Вариант 1)  
**Время:** 2-3 минуты

После выполнения Варианта 1 и перезапуска сервиса всё должно заработать! 🎉

