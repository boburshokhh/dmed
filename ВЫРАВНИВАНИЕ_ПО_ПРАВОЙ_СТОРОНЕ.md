# Выравнивание PIN-кода и QR-кода по правой стороне ячейки

## ✅ Изменения

### Выравнивание изменено с LEFT на RIGHT

**Было:**
- PIN-код и QR-код выровнены по **левой стороне** ячейки

**Стало:**
- PIN-код и QR-код выровнены по **правой стороне** ячейки

---

## 🔧 Технические изменения

### 1. Выравнивание PIN-кода

**Было:**
```python
para_pin.alignment = WD_ALIGN_PARAGRAPH.LEFT
para_pin_format.left_indent = Pt(0)  # Прижимаем к левому краю
```

**Стало:**
```python
para_pin.alignment = WD_ALIGN_PARAGRAPH.RIGHT
para_pin_format.right_indent = Pt(0)  # Прижимаем к правому краю
```

### 2. Выравнивание QR-кода

**Было:**
```python
para_qr.alignment = WD_ALIGN_PARAGRAPH.LEFT
para_qr_format.left_indent = Pt(0)  # Прижимаем к левому краю
```

**Стало:**
```python
para_qr.alignment = WD_ALIGN_PARAGRAPH.RIGHT
para_qr_format.right_indent = Pt(0)  # Прижимаем к правому краю
```

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До изменения (по левому краю):
```
┌─────────────────────────────────────┐
│  2653       [QR-код]                │  ← По левому краю
│                                     │
└─────────────────────────────────────┘
```

### После изменения (по правому краю):
```
┌─────────────────────────────────────┐
│               2653       [QR-код]   │  ← По правому краю
│                                     │
└─────────────────────────────────────┘
```

---

## 🎯 Результат

После изменений:

✅ **PIN-код выровнен по правому краю** ячейки  
✅ **QR-код выровнен по правому краю** ячейки  
✅ **Оба элемента прижаты к правой стороне**  
✅ **Близко друг к другу** (минимальное расстояние между ними)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Параметры остались прежними

- **Ширина ячеек**: PIN=2.0 см, QR=3.8 см
- **Размер QR-кода**: 1.1 дюйма (~2.79 см)
- **Размер шрифта PIN-кода**: 20pt
- **Вертикальное выравнивание**: CENTER (не изменилось)
- **Отступы**: Минимальные (0 для top, bottom, right; 20 twips для left между элементами)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код и QR-код выровнены по правой стороне

---

## 📝 Резюме

**Изменение:** Выравнивание с LEFT на RIGHT  
**Применено к:** PIN-код и QR-код в обеих таблицах  
**Результат:** Оба элемента выровнены по правой стороне ячейки ✅

После применения изменений PIN-код и QR-код будут выровнены по правой стороне ячейки!

