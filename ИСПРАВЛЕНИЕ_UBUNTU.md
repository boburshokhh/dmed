# Исправление проблемы qrcode-styled на Ubuntu сервере

## 🔍 Проблема найдена!

**Причина:** Библиотеки `qrcode-styled` и `Pillow` не установлены в виртуальном окружении на Ubuntu сервере.

## ✅ Решение (3 способа)

### Способ 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

1. Загрузите файл `FIX_UBUNTU_QRCODE.sh` на сервер
2. Выполните:

```bash
cd /var/www/dmed
chmod +x FIX_UBUNTU_QRCODE.sh
sudo bash FIX_UBUNTU_QRCODE.sh
```

Скрипт автоматически:
- Найдёт или создаст виртуальное окружение
- Установит все необходимые библиотеки
- Проверит работоспособность

---

### Способ 2: Ручная установка (если скрипт не работает)

Выполните эти команды **по порядку**:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/dmed

# 2. Активируйте виртуальное окружение (если есть)
# Если есть папка venv:
source venv/bin/activate

# Если есть папка .venv:
source .venv/bin/activate

# Если есть папка env:
source env/bin/activate

# Если виртуального окружения нет, создайте его:
python3 -m venv venv
source venv/bin/activate

# 3. Обновите pip
python3 -m pip install --upgrade pip

# 4. Установите системные зависимости (если нужно)
sudo apt-get update
sudo apt-get install -y python3-dev python3-pip libjpeg-dev zlib1g-dev

# 5. Установите библиотеки из requirements.txt
python3 -m pip install -r requirements.txt

# ИЛИ установите вручную:
python3 -m pip install qrcode-styled>=0.2.0 Pillow>=10.0.0

# 6. Проверьте установку
python3 -c "import qrcode_styled; from PIL import Image; print('OK!')"
```

---

### Способ 3: Быстрая установка (только нужные библиотеки)

```bash
cd /var/www/dmed
source venv/bin/activate  # или ваш путь к venv
pip3 install qrcode-styled Pillow
```

---

## 🔄 После установки

### 1. Перезапустите приложение

**Если используете systemd:**
```bash
sudo systemctl restart dmed
```

**Если запускаете вручную:**
```bash
cd /var/www/dmed
source venv/bin/activate  # важно!
python3 app.py
```

### 2. Проверьте логи

```bash
# Для systemd:
sudo journalctl -u dmed -f

# Или проверьте последние логи:
sudo journalctl -u dmed -n 50 | grep -i qr
```

### 3. Проверьте работу

Создайте тестовый документ через веб-интерфейс и проверьте, что QR-код генерируется.

---

## ✅ Проверка успешной установки

Выполните эту команду для проверки:

```bash
cd /var/www/dmed
source venv/bin/activate  # активируйте venv!
python3 -c "
from qrcode_styled import QRCodeStyled
from PIL import Image
qr = QRCodeStyled()
img = qr.get_image('test')
print('✓ ВСЁ РАБОТАЕТ!')
print(f'Размер QR-кода: {img.size if hasattr(img, \"size\") else \"OK\"}')"
```

Если видите `✓ ВСЁ РАБОТАЕТ!` - значит проблема решена!

---

## ⚠️ Важные моменты

1. **Всегда активируйте виртуальное окружение** перед установкой библиотек
2. **Используйте `python3 -m pip`** вместо просто `pip` для надёжности
3. **Проверьте, что приложение запускается из виртуального окружения**

---

## 🆘 Если проблема осталась

1. Проверьте, что виртуальное окружение активировано:
   ```bash
   which python3
   # Должен показать путь к venv, например: /var/www/dmed/venv/bin/python3
   ```

2. Проверьте установленные библиотеки:
   ```bash
   source venv/bin/activate
   pip3 list | grep -E "(qrcode|Pillow)"
   ```

3. Проверьте пути Python в приложении:
   ```bash
   source venv/bin/activate
   python3 -c "import sys; print('\n'.join(sys.path))"
   ```

---

## 📝 Резюме

**Проблема:** Библиотеки не установлены  
**Решение:** Установить через `pip install -r requirements.txt` в виртуальном окружении  
**Время:** 2-3 минуты

После выполнения этих шагов проблема должна быть решена! 🎉

