# Исправление обрезки QR-кода и увеличение сдвига вправо

## ✅ Проблема

1. **Правый край QR-кода обрезался** - не был виден полностью
2. **Нужно сдвинуть ещё немного вправо** - увеличить сдвиг

## 🔧 Решение

1. **Увеличена ширина ячейки QR-кода** с `3.8 см` до `4.2 см`
2. **Увеличен отрицательный right_indent** с `-300 twips` (~20px) до `-360 twips` (~24px) для большего сдвига вправо

---

## 📋 Технические изменения

### 1. Увеличена ширина ячейки QR-кода

**Было:**
```python
table.columns[1].width = Cm(3.8)  # Ширина 3.8 см
```

**Стало:**
```python
table.columns[1].width = Cm(4.2)  # Ширина 4.2 см (увеличена на 0.4 см)
```

**Причина:** 
- QR-код размером 1.1 дюйма (~2.79 см) + отрицательный right_indent (-360 twips ≈ 0.48 см) + отступы = нужно минимум 4.2 см
- Старая ширина 3.8 см была недостаточной, QR-код обрезался справа

### 2. Увеличен отрицательный right_indent

**Было:**
```python
ind.set(qn('w:right'), '-300')  # ~20px сдвиг
```

**Стало:**
```python
ind.set(qn('w:right'), '-360')  # ~24px сдвиг (увеличен на 4px)
```

**Применено к:**
- ✅ Обычная таблица - ячейка PIN
- ✅ Обычная таблица - ячейка QR
- ✅ Вложенная таблица - ячейка PIN
- ✅ Вложенная таблица - ячейка QR

### 3. Обновлены комментарии

Обновлены комментарии о размере ячейки с `3.8 см` на `4.2 см`.

---

## 📊 Визуальное представление

### До (QR-код обрезался):
```
┌─────────────────────────────────────┐
│                        2653 [QR]   │  ← Правый край QR-кода обрезан
│                                     │
└─────────────────────────────────────┘
Ширина ячейки: 3.8 см (недостаточно)
```

### После (QR-код полностью виден, сдвинут больше):
```
┌─────────────────────────────────────┐
│                     2653 [QR]      │  ← QR-код полностью виден, сдвинут больше
│                                     │
└─────────────────────────────────────┘
Ширина ячейки: 4.2 см (достаточно)
```

---

## 🎯 Результат

После изменений:

✅ **QR-код полностью виден** - правый край не обрезается  
✅ **PIN-код и QR-код сдвинуты больше вправо** (~24px вместо ~20px)  
✅ **Оба элемента вместе** (одинаковый сдвиг)  
✅ **Близко друг к другу** (минимальное расстояние сохранено)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Технические детали

### Конвертация единиц:
- **24px** ≈ **18pt** ≈ **360 twips**
- **0.4 см** = дополнительная ширина ячейки

### Расчет ширины ячейки:
- **QR-код**: 1.1 дюйма = ~2.79 см
- **Отрицательный right_indent**: -360 twips ≈ 0.48 см
- **Отступы ячейки**: ~0.2 см (left margin для сближения с PIN)
- **Запас**: ~0.7 см
- **Итого**: 2.79 + 0.48 + 0.2 + 0.7 = **~4.2 см** ✅

### Механизм работы:
1. **Увеличенная ширина ячейки** (4.2 см) обеспечивает достаточно места для QR-кода
2. **Отрицательный right_indent** (-360 twips) сдвигает содержимое правее
3. **QR-код не обрезается** благодаря увеличенной ширине
4. **Оба элемента сдвинуты больше** благодаря увеличенному отрицательному right_indent

---

## 📐 Текущие параметры

### Ширина ячеек:
- **PIN**: 2.0 см (не изменилась)
- **QR**: 4.2 см (увеличена с 3.8 см)

### Размер QR-кода:
- **1.1 дюйма** (~2.79 см) - не изменился

### Отрицательный right_indent:
- **-360 twips** (~24px) - увеличен с -300 twips (~20px)

### Отступы ячеек (tcMar):
- **Top**: 0
- **Bottom**: 0
- **Left**: 0 (для PIN), 20 twips (для QR - минимальный для сближения)
- **Right**: 0 (для обеих ячеек)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что:
   - QR-код полностью виден (правый край не обрезается)
   - PIN-код и QR-код сдвинуты больше вправо

---

## ⚠️ Важные замечания

1. **Увеличенная ширина ячейки** может повлиять на общую ширину таблицы
2. Если нужно ещё больше сдвига:
   - Увеличьте значение `-360` до `-420` или `-480` (больше сдвиг)
   - Или уменьшите до `-300` (меньше сдвиг)

3. **Если QR-код всё ещё обрезается:**
   - Увеличьте ширину ячейки до `4.5 см` или `5.0 см`
   - Или уменьшите размер QR-кода до `1.0 дюйма`

4. **Если результат не соответствует ожиданиям:**
   - Проверьте версию LibreOffice/Word
   - Убедитесь, что XML правильно применяется (проверьте логи)
   - Попробуйте изменить значения ширины ячейки или отрицательного right_indent

---

## 📝 Резюме

**Проблема 1:** Правый край QR-кода обрезался  
**Решение 1:** Увеличена ширина ячейки QR-кода с 3.8 см до 4.2 см  

**Проблема 2:** Нужно сдвинуть ещё немного вправо  
**Решение 2:** Увеличен отрицательный right_indent с -300 twips до -360 twips  

**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** QR-код полностью виден, оба элемента сдвинуты больше вправо ✅

После применения изменений QR-код будет полностью виден, а PIN-код и QR-код будут сдвинуты больше вправо!

