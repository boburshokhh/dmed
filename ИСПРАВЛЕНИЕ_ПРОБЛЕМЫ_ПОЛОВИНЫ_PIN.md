# Исправление проблемы: PIN-код отображается в две строки (половина)

## 🔍 Проблема

PIN-код отображается в две строки вместо одной:
- Первая строка: "791"
- Вторая строка: "7"

**Вместо:** "7917" в одной строке

---

## 🔎 Причины проблемы

### Причина 1: Неполная очистка ячейки
**Проблема:** `cell.text = ''` не полностью удаляет все параграфы из ячейки. Если PIN-код был уже заменен в шаблоне и разбит на несколько параграфов, они могут остаться.

**Решение:** Полностью удаляем все параграфы из ячейки перед созданием таблицы:
```python
for paragraph in cell.paragraphs[:]:
    p_element = paragraph._element
    p_element.getparent().remove(p_element)
```

### Причина 2: Недостаточная ширина ячейки
**Проблема:** Ширина 1.5 см может быть недостаточной для 4-значного PIN-кода с размером шрифта 20pt, особенно если в шаблоне есть дополнительные отступы.

**Решение:** Увеличена ширина до 2.0 см:
```python
inner_table.columns[0].width = Cm(2.0)  # Было: Cm(1.5)
```

### Причина 3: PIN-код может содержать пробелы или переносы
**Проблема:** Если PIN-код приходит с пробелами или переносами строк, он может разбиваться.

**Решение:** Очищаем PIN-код от всех пробелов и переносов:
```python
pin_text = str(pin_code).strip()
pin_text = pin_text.replace('\n', '').replace('\r', '').replace(' ', '')
```

### Причина 4: Параграф не очищен перед добавлением
**Проблема:** Параграф может содержать старый текст.

**Решение:** Очищаем параграф перед добавлением PIN-кода:
```python
para_pin.clear()
```

---

## ✅ Все исправления применены

### 1. Полная очистка ячейки
```python
# Полностью очищаем ячейку - удаляем все параграфы
for paragraph in cell.paragraphs[:]:
    p_element = paragraph._element
    p_element.getparent().remove(p_element)
```

### 2. Увеличена ширина ячейки
```python
# Было:
inner_table.columns[0].width = Cm(1.5)

# Стало:
inner_table.columns[0].width = Cm(2.0)  # Достаточно для 4 цифр
```

### 3. Очистка PIN-кода от пробелов и переносов
```python
pin_text = str(pin_code).strip()
pin_text = pin_text.replace('\n', '').replace('\r', '').replace(' ', '')
```

### 4. Очистка параграфа
```python
para_pin.clear()  # Очищаем перед добавлением
```

### 5. Три уровня защиты от переноса
- ✅ `noWrap` на ячейке таблицы
- ✅ `noBreak` на run
- ✅ `keep_together` на параграфе

---

## 📋 Проверка шаблона

### Проблема может быть в шаблоне, если:

1. **PIN-код разбит на несколько строк в шаблоне:**
   ```
   {{pin_code}}
   ```
   Если в шаблоне есть перенос строки внутри или вокруг плейсхолдера, это может вызвать проблему.

2. **В шаблоне есть форматирование, которое разбивает текст:**
   - Принудительные переносы строк
   - Специальные символы
   - Неправильное выравнивание

### Как проверить шаблон:

1. Откройте шаблон в LibreOffice Writer
2. Найдите ячейку с `{{pin_code}}` и `{{qr_code}}`
3. Проверьте:
   - Нет ли принудительных переносов строк (Shift+Enter)
   - Нет ли лишних пробелов
   - Правильно ли выровнена ячейка

### Правильный шаблон:

В ячейке таблицы должно быть:
```
{{pin_code}} {{qr_code}}
```

**Или:**
```
{{pin_code}}
{{qr_code}}
```

**Важно:** Оба плейсхолдера должны быть в одной ячейке, без принудительных переносов строк между ними.

---

## 🛠️ Что делать, если проблема осталась

### Шаг 1: Проверьте шаблон

1. Откройте шаблон в LibreOffice Writer
2. Найдите ячейку с PIN и QR-кодом
3. Убедитесь, что:
   - Нет принудительных переносов строк
   - Нет лишних пробелов
   - Плейсхолдеры в одной ячейке

### Шаг 2: Пересоздайте ячейку в шаблоне

Если проблема в шаблоне:

1. Удалите содержимое ячейки
2. Введите заново: `{{pin_code}} {{qr_code}}`
3. Сохраните шаблон
4. Попробуйте снова

### Шаг 3: Проверьте логи

Проверьте логи на наличие сообщений:
```
[QR_PIN_LAYOUT] Ячейка полностью очищена от всех параграфов
[QR_PIN_LAYOUT] PIN-код добавлен как одна строка: '7917'
```

Если видите эти сообщения - код работает правильно, проблема может быть в шаблоне.

---

## 📊 Итоговые параметры

### Ширина ячеек:
- **PIN-код**: 2.0 см (увеличена с 1.5 см)
- **QR-код**: 2.8 см

### Защита от переноса:
1. ✅ Полная очистка ячейки перед созданием таблицы
2. ✅ Очистка PIN-кода от пробелов и переносов
3. ✅ Очистка параграфа перед добавлением
4. ✅ `noWrap` на ячейке
5. ✅ `noBreak` на run
6. ✅ `keep_together` на параграфе

---

## 🎯 Результат

После исправлений:

✅ **PIN-код отображается в одну строку** (например: "7917", "0040")  
✅ **Не разбивается на две строки**  
✅ **Выровнен по левому краю** ячейки  
✅ **По центру вертикально**  
✅ **Близко к QR-коду**

---

## ⚠️ Если проблема осталась

Если после всех исправлений PIN-код всё ещё разбивается:

1. **Проверьте шаблон** - возможно, проблема в форматировании шаблона
2. **Проверьте логи** - найдите сообщения `[QR_PIN_LAYOUT]`
3. **Проверьте размер шрифта** - возможно, нужно уменьшить до 18pt
4. **Проверьте ширину ячейки** - возможно, нужно увеличить до 2.5 см

---

## 📝 Резюме

**Проблема:** PIN-код разбивается на две строки  
**Причины:** Неполная очистка ячейки, недостаточная ширина, пробелы в PIN-коде  
**Решение:** Полная очистка ячейки, увеличение ширины до 2.0 см, очистка PIN-кода  
**Результат:** PIN-код в одну строку, правильно расположен

После применения исправлений и проверки шаблона проблема должна быть решена! ✅

