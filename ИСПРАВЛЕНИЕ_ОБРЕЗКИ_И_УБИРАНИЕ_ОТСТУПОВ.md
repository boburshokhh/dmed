# Исправление обрезки QR-кода и убирание отступов между PIN и QR

## ✅ Проблемы

1. **Половина правой стороны QR-кода не видна** - QR-код все еще обрезается
2. **Между PIN-кодом и QR-кодом есть отступы** - нужно сделать их ближе друг к другу

## 🔧 Решение

1. **Увеличена ширина ячейки QR-кода** с `4.2 см` до `5.0 см`
2. **Убраны отступы между PIN и QR** - left margin ячейки QR и right margin ячейки PIN установлены в `0`

---

## 📋 Технические изменения

### 1. Увеличена ширина ячейки QR-кода

**Было:**
```python
table.columns[1].width = Cm(4.2)  # Ширина 4.2 см
```

**Стало:**
```python
table.columns[1].width = Cm(5.0)  # Ширина 5.0 см (увеличена на 0.8 см)
```

**Причина:** 
- QR-код размером 1.1 дюйма (~2.79 см) + отрицательный right_indent (-360 twips ≈ 0.48 см) + отступы = нужно минимум 5.0 см
- Старая ширина 4.2 см была недостаточной, QR-код все еще обрезался справа

### 2. Убраны отступы между PIN и QR

**Было:**
```python
# Ячейка PIN - right margin
right_margin.set(qn('w:w'), '0')  # Уже было 0

# Ячейка QR - left margin
left_margin.set(qn('w:w'), '20')  # Отступ 20 twips (~2-3px)
```

**Стало:**
```python
# Ячейка PIN - right margin
right_margin.set(qn('w:w'), '0')  # Нулевой отступ - прижимаем к QR-коду

# Ячейка QR - left margin
left_margin.set(qn('w:w'), '0')  # Нулевой отступ - прижимаем к PIN-коду
```

**Применено к:**
- ✅ Обычная таблица - ячейка PIN (right margin)
- ✅ Обычная таблица - ячейка QR (left margin)
- ✅ Вложенная таблица - ячейка PIN (right margin)
- ✅ Вложенная таблица - ячейка QR (left margin)

### 3. Обновлены комментарии

Обновлены комментарии о размере ячейки с `4.2 см` на `5.0 см`.

---

## 📊 Визуальное представление

### До (QR-код обрезался, были отступы):
```
┌─────────────────────────────────────┐
│           2653    [QR]             │  ← Отступ между PIN и QR
│                                     │  ← Правый край QR-кода обрезан
└─────────────────────────────────────┘
Ширина ячейки QR: 4.2 см (недостаточно)
Left margin QR: 20 twips (отступ)
```

### После (QR-код полностью виден, без отступов):
```
┌─────────────────────────────────────┐
│          2653[QR]                  │  ← Нет отступа между PIN и QR
│                                     │  ← QR-код полностью виден
└─────────────────────────────────────┘
Ширина ячейки QR: 5.0 см (достаточно)
Left margin QR: 0 (нет отступа)
```

---

## 🎯 Результат

После изменений:

✅ **QR-код полностью виден** - правый край не обрезается  
✅ **PIN-код и QR-код максимально близко** - нет отступов между ними  
✅ **Оба элемента вместе** (одинаковый сдвиг вправо)  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Технические детали

### Расчет ширины ячейки:
- **QR-код**: 1.1 дюйма = ~2.79 см
- **Отрицательный right_indent**: -360 twips ≈ 0.48 см
- **Отступы ячейки**: 0 см (left margin = 0)
- **Запас**: ~1.7 см
- **Итого**: 2.79 + 0.48 + 0 + 1.7 = **~5.0 см** ✅

### Отступы между элементами:
- **Right margin ячейки PIN**: 0 (прижато к QR-коду)
- **Left margin ячейки QR**: 0 (прижато к PIN-коду)
- **Итого**: 0 + 0 = **0 отступов** ✅

---

## 📐 Текущие параметры

### Ширина ячеек:
- **PIN**: 2.0 см (не изменилась)
- **QR**: 5.0 см (увеличена с 4.2 см)

### Размер QR-кода:
- **1.1 дюйма** (~2.79 см) - не изменился

### Отрицательный right_indent:
- **-360 twips** (~24px) - не изменился

### Отступы ячеек (tcMar):
- **Top**: 0
- **Bottom**: 0
- **Left**: 0 (для PIN), **0** (для QR - убран отступ 20 twips)
- **Right**: **0** (для PIN - прижато к QR), 0 (для QR)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что:
   - QR-код полностью виден (правый край не обрезается)
   - PIN-код и QR-код максимально близко друг к другу (нет отступов)

---

## ⚠️ Важные замечания

1. **Увеличенная ширина ячейки** (5.0 см) может повлиять на общую ширину таблицы
2. Если QR-код всё ещё обрезается:
   - Увеличьте ширину ячейки до `5.5 см` или `6.0 см`
   - Или уменьшите размер QR-кода до `1.0 дюйма`

3. **Если нужно добавить небольшой отступ между PIN и QR:**
   - Установите left margin ячейки QR в `10 twips` или `15 twips` (вместо 0)

4. **Если результат не соответствует ожиданиям:**
   - Проверьте версию LibreOffice/Word
   - Убедитесь, что XML правильно применяется (проверьте логи)
   - Попробуйте изменить значения ширины ячейки или отступов

---

## 📝 Резюме

**Проблема 1:** Половина правой стороны QR-кода не видна  
**Решение 1:** Увеличена ширина ячейки QR-кода с 4.2 см до 5.0 см  

**Проблема 2:** Между PIN-кодом и QR-кодом есть отступы  
**Решение 2:** Убраны отступы - left margin ячейки QR и right margin ячейки PIN установлены в 0  

**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** QR-код полностью виден, PIN-код и QR-код максимально близко друг к другу ✅

После применения изменений QR-код будет полностью виден, а PIN-код и QR-код будут максимально близко друг к другу без отступов!

