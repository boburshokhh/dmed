# Исправления расположения PIN-кода и QR-кода

## 🔍 Найденные ошибки

### Ошибка 1: Слишком большая ширина колонок
**Проблема:**
- PIN-код: 1.5 см (слишком широко)
- QR-код: 3.5-3.6 см (слишком широко)
- Элементы были далеко друг от друга

**Исправление:**
- PIN-код: уменьшено до **1.0 см** (минимальная ширина для 4 цифр)
- QR-код: уменьшено до **2.8 см** (компактная ширина)
- Элементы теперь ближе друг к другу

---

### Ошибка 2: Неправильное выравнивание PIN-кода
**Проблема:**
- PIN-код был выровнен по **CENTER** в ячейке
- Не соответствовал требованию "по левому краю"

**Исправление:**
- Изменено на **LEFT** (по левому краю ячейки)
- Теперь соответствует требованиям

---

### Ошибка 3: Нулевые отступы между элементами
**Проблема:**
- Отступы между PIN и QR были установлены в **0**
- Элементы могли "слипаться" визуально

**Исправление:**
- Установлен минимальный отступ **20 twips** (~2-3 пикселя)
- Элементы близко, но не слипаются

---

### Ошибка 4: Слишком большой размер QR-кода
**Проблема:**
- QR-код был **1.4 дюйма** (слишком большой)
- Занимал много места

**Исправление:**
- Уменьшен до **1.2 дюйма**
- Более компактное размещение

---

### Ошибка 5: Неправильный размер шрифта PIN-кода
**Проблема:**
- Размер шрифта был **24pt** (в вложенной таблице) и **18pt** (в обычной)
- Неоптимальный размер

**Исправление:**
- Установлен единый размер **20pt**
- Лучшая читаемость и компактность

---

### Ошибка 6: Отсутствие выравнивания вложенной таблицы
**Проблема:**
- Вложенная таблица не имела явного выравнивания
- Могла быть выровнена не по левому краю

**Исправление:**
- Добавлено явное выравнивание **LEFT** для вложенной таблицы
- Таблица всегда выровнена по левому краю родительской ячейки

---

## ✅ Все исправления применены

### Изменения в коде:

1. **Ширина колонок** (строки 713-715, 839-841):
   ```python
   # Было:
   table.columns[0].width = Cm(1.5)  # PIN
   table.columns[1].width = Cm(3.6)   # QR
   
   # Стало:
   table.columns[0].width = Cm(1.0)  # PIN
   table.columns[1].width = Cm(2.8)   # QR
   ```

2. **Выравнивание PIN-кода** (строки 752, 876):
   ```python
   # Было:
   para_pin.alignment = WD_ALIGN_PARAGRAPH.CENTER
   
   # Стало:
   para_pin.alignment = WD_ALIGN_PARAGRAPH.LEFT
   ```

3. **Отступы между элементами** (строки 741-744, 867-870, 789-792, 911-914):
   ```python
   # Было:
   right_margin.set(qn('w:w'), '0')  # Нулевой отступ
   
   # Стало:
   right_margin.set(qn('w:w'), '20')  # Минимальный отступ ~2-3px
   ```

4. **Размер QR-кода** (строки 807, 927, 933):
   ```python
   # Было:
   qr_width_inches = 1.4
   
   # Стало:
   qr_width_inches = 1.2
   ```

5. **Размер шрифта PIN-кода** (строки 760, 884):
   ```python
   # Было:
   run_pin.font.size = Pt(18)  # или Pt(24)
   
   # Стало:
   run_pin.font.size = Pt(20)  # Единый размер
   ```

6. **Выравнивание вложенной таблицы** (строка 837):
   ```python
   # Добавлено:
   inner_table.alignment = WD_ALIGN_PARAGRAPH.LEFT
   ```

---

## 📊 Сравнение: До и После

### До исправлений:
- ❌ Ширина: PIN=1.5см, QR=3.6см (слишком широко)
- ❌ Выравнивание: PIN по центру (неправильно)
- ❌ Отступы: 0 (слипаются)
- ❌ QR-код: 1.4 дюйма (слишком большой)
- ❌ Шрифт: 18-24pt (неоптимально)

### После исправлений:
- ✅ Ширина: PIN=1.0см, QR=2.8см (компактно)
- ✅ Выравнивание: PIN по левому краю (правильно)
- ✅ Отступы: 20 twips (~2-3px) (близко, но не слипаются)
- ✅ QR-код: 1.2 дюйма (компактно)
- ✅ Шрифт: 20pt (оптимально)

---

## 🎯 Итоговый результат

После всех исправлений:

✅ **PIN-код и QR-код расположены горизонтально** (рядом друг с другом)  
✅ **Выровнены по левому краю** ячейки  
✅ **Выровнены по центру вертикально**  
✅ **Близко друг к другу** (минимальное расстояние ~2-3px)  
✅ **Компактные размеры** (занимают меньше места)  
✅ **Единообразное форматирование** (одинаковые размеры во всех местах)

---

## 📝 Как использовать в шаблоне

В ячейке таблицы разместите:

```
{{pin_code}} {{qr_code}}
```

**Результат:**
- PIN-код слева, QR-код справа
- Выровнены по левому краю
- По центру вертикально
- Близко друг к другу

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте расположение PIN-кода и QR-кода

После этого всё должно работать правильно! 🎉

