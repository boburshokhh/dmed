#!/bin/bash
# Скрипт для создания и настройки виртуального окружения

set -e

# Получаем директорию скрипта
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"

# Проверяем, существует ли уже виртуальное окружение
if [ -d "venv" ]; then
    echo "✅ Виртуальное окружение уже существует"
    source venv/bin/activate
else
    echo "📦 Создаем виртуальное окружение..."
    python3 -m venv venv
    source venv/bin/activate
fi

# Обновляем pip
echo "⬆️  Обновляем pip..."
pip install --upgrade pip

# Устанавливаем зависимости
echo "📥 Устанавливаем зависимости из requirements.txt..."
pip install -r requirements.txt

echo ""
echo "✅ Готово! Виртуальное окружение настроено и активировано."
echo ""
echo "Для активации в будущем используйте:"
echo "   source venv/bin/activate"
echo ""
echo "Для запуска приложения используйте:"
echo "   bash start.sh"
echo "   или"
echo "   source venv/bin/activate && python app.py"
