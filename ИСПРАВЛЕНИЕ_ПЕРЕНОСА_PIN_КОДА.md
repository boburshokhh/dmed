# Исправление переноса PIN-кода на две строки

## 🔍 Проблема

PIN-код отображался в две строки:
- Первая строка: "19"
- Вторая строка: "13"

**Вместо:** "1913" в одной строке

---

## ✅ Исправления

### 1. Увеличена ширина ячейки для PIN-кода

**Было:**
```python
inner_table.columns[0].width = Cm(1.0)  # Слишком узко
```

**Стало:**
```python
inner_table.columns[0].width = Cm(1.5)  # Достаточная ширина для 4 цифр
```

**Причина:** Ширина 1.0 см была недостаточной для отображения 4 цифр в одну строку при размере шрифта 20pt.

---

### 2. Добавлен атрибут noBreak для предотвращения переноса

**Добавлено:**
```python
# Добавляем атрибут noBreak для предотвращения переноса внутри run
r_pr = run_pin._element.get_or_add_rPr()
no_break = OxmlElement('w:noBreak')
r_pr.append(no_break)
```

**Причина:** Атрибут `noBreak` предотвращает разрыв текста внутри run, даже если Word пытается перенести его.

---

### 3. Улучшены настройки параграфа

**Добавлено:**
```python
para_pin_format.widow_control = False
para_pin_format.keep_together = True
```

**Причина:** Эти настройки помогают удерживать текст вместе и предотвращают перенос.

---

### 4. Уже было установлено noWrap для ячейки

**Уже было:**
```python
no_wrap_pin = OxmlElement('w:noWrap')
tc_pr_pin.append(no_wrap_pin)
```

**Причина:** `noWrap` на уровне ячейки предотвращает перенос текста в ячейке таблицы.

---

## 📊 Итоговые параметры

### Ширина ячеек:
- **PIN-код**: 1.5 см (достаточно для 4 цифр)
- **QR-код**: 2.8 см (компактно)

### Защита от переноса (3 уровня):
1. ✅ **noWrap на ячейке** - предотвращает перенос в ячейке таблицы
2. ✅ **noBreak на run** - предотвращает разрыв внутри текста
3. ✅ **keep_together на параграфе** - удерживает текст вместе

### Размер шрифта:
- **PIN-код**: 20pt (жирный, Arial)

---

## 🎯 Результат

После исправлений:

✅ **PIN-код отображается в одну строку** (например: "1913")  
✅ **Не переносится на две строки**  
✅ **Выровнен по левому краю** ячейки  
✅ **По центру вертикально**  
✅ **Близко к QR-коду** (минимальное расстояние)

---

## 📝 Визуальное представление

### До исправления:
```
┌─────────────┬─────────────┐
│     19      │             │
│     13      │  [QR-код]   │
└─────────────┴─────────────┘
```

### После исправления:
```
┌─────────────┬─────────────┐
│  1913       │  [QR-код]   │
│             │             │
└─────────────┴─────────────┘
```

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код отображается в одну строку

---

## ⚠️ Важные замечания

1. **Ширина ячейки критична** - если уменьшить до 1.0 см, PIN-код может снова переноситься
2. **Размер шрифта** - 20pt оптимален для ширины 1.5 см
3. **Три уровня защиты** - noWrap + noBreak + keep_together обеспечивают надёжную защиту от переноса

---

## ✅ Проверка

После применения исправлений проверьте:

- ✅ PIN-код отображается в одну строку (например: "1913", "0040")
- ✅ Не разбивается на две строки
- ✅ Выровнен по левому краю
- ✅ Близко к QR-коду
- ✅ По центру вертикально

Если всё соответствует - проблема решена! ✅

