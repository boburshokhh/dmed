# PIN-код и QR-код по правому краю без отступов

## ✅ Изменения

### Убраны все отступы, элементы прижаты к правому краю ячейки

**Было:**
- Right margin ячейки: `-240 twips` (отрицательный, сдвиг на 20px правее)

**Стало:**
- Right margin ячейки: `0` (нулевой, прижато к правому краю без отступов)

---

## 🔧 Технические изменения

### 1. Right margin ячейки PIN-кода

**Было:**
```python
right_margin_pin.set(qn('w:w'), '-240')  # Отрицательный отступ ~20px
```

**Стало:**
```python
right_margin_pin.set(qn('w:w'), '0')  # Нулевой отступ - прижимаем к правому краю
```

### 2. Right margin ячейки QR-кода

**Было:**
```python
right_margin_qr.set(qn('w:w'), '-240')  # Отрицательный отступ ~20px
```

**Стало:**
```python
right_margin_qr.set(qn('w:w'), '0')  # Нулевой отступ - прижимаем к правому краю
```

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До изменения (с отступом 20px):
```
┌─────────────────────────────────────┐
│                        2653 [QR]   │  ← Сдвинуто на 20px правее
│                                     │
└─────────────────────────────────────┘
```

### После изменения (без отступов):
```
┌─────────────────────────────────────┐
│                           2653 [QR] │  ← Прижато к правому краю без отступов
│                                     │
└─────────────────────────────────────┘
```

---

## 🎯 Результат

После изменений:

✅ **PIN-код прижат к правому краю** ячейки без отступов  
✅ **QR-код прижат к правому краю** ячейки без отступов  
✅ **Оба элемента вместе** (минимальное расстояние между ними сохранено)  
✅ **Без внутренних отступов** в ячейке  
✅ **По центру вертикально** (вертикальное выравнивание не изменилось)

---

## 📐 Текущие параметры

### Отступы ячеек:
- **Top**: 0
- **Bottom**: 0
- **Left**: 0 (для PIN), 20 twips (для QR - минимальный для сближения)
- **Right**: 0 (для обеих ячеек)

### Отступы параграфов:
- **space_before**: 0
- **space_after**: 0
- **left_indent**: 0
- **right_indent**: 0

### Выравнивание:
- **Горизонтальное**: RIGHT (по правому краю)
- **Вертикальное**: CENTER (по центру)

### Размеры:
- **Ширина ячеек**: PIN=2.0 см, QR=3.8 см
- **Размер QR-кода**: 1.1 дюйма (~2.79 см)
- **Размер шрифта PIN-кода**: 20pt

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что PIN-код и QR-код прижаты к правому краю без отступов

---

## 📝 Резюме

**Изменение:** Убраны отрицательные right margin, установлены в 0  
**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** Оба элемента прижаты к правому краю ячейки без внутренних отступов ✅

После применения изменений PIN-код и QR-код будут прижаты к правому краю ячейки без отступов!

