# Исправление переноса PIN-кода и сдвиг вправо

## ✅ Проблемы

1. **PIN-код переносился на две строки** (например, "67" и "58" вместо "6758")
2. **PIN-код нужно сдвинуть вправо** (как и QR-код)

## 🔧 Решение

1. **Увеличена ширина ячейки PIN-кода** с `2.0 см` до `3.0 см`
2. **Убран дублирующий код noBreak**
3. **Подтверждено применение left_indent = Pt(25)** для сдвига вправо

---

## 📋 Технические изменения

### 1. Увеличена ширина ячейки PIN-кода

**Было:**
```python
table.columns[0].width = Cm(2.0)  # Ширина 2.0 см
```

**Стало:**
```python
table.columns[0].width = Cm(3.0)  # Ширина 3.0 см (увеличена на 1.0 см)
```

**Причина:** 
- PIN-код размером 20pt + left_indent 25px (~0.66 см) + отступы = нужно минимум 3.0 см
- Старая ширина 2.0 см была недостаточной, PIN-код переносился на две строки

### 2. Убран дублирующий код noBreak

**Было:**
```python
# Дублирование noBreak
r_pr = run_pin._element.get_or_add_rPr()
no_break = OxmlElement('w:noBreak')
r_pr.append(no_break)
# ... позже еще раз ...
r_pr = run_pin._element.get_or_add_rPr()
no_break = OxmlElement('w:noBreak')
r_pr.append(no_break)
```

**Стало:**
```python
# Один раз noBreak
r_pr = run_pin._element.get_or_add_rPr()
no_break = OxmlElement('w:noBreak')
r_pr.append(no_break)
```

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До (PIN-код переносился):
```
┌─────────────────────────────────────┐
│           67                        │  ← PIN-код разбит на две строки
│           58                        │
│           [QR]                      │
└─────────────────────────────────────┘
Ширина ячейки PIN: 2.0 см (недостаточно)
```

### После (PIN-код в одной строке, сдвинут вправо):
```
┌─────────────────────────────────────┐
│            6758 [QR]                │  ← PIN-код в одной строке, сдвинут вправо
└─────────────────────────────────────┘
Ширина ячейки PIN: 3.0 см (достаточно)
Left_indent: Pt(25) (~25px)
```

---

## 🎯 Результат

После изменений:

✅ **PIN-код в одной строке** - не переносится на две строки  
✅ **PIN-код сдвинут вправо** на ~25px (left_indent = Pt(25))  
✅ **QR-код сдвинут вправо** на ~25px (left_indent = Pt(25))  
✅ **Оба элемента вместе** (одинаковый сдвиг)  
✅ **Близко друг к другу** (минимальное расстояние сохранено)

---

## 📐 Технические детали

### Расчет ширины ячейки:
- **PIN-код**: 4 цифры × ~0.5 см = ~2.0 см
- **Left_indent**: Pt(25) ≈ 0.66 см
- **Отступы ячейки**: ~0.1 см
- **Запас**: ~0.2 см
- **Итого**: 2.0 + 0.66 + 0.1 + 0.2 = **~3.0 см** ✅

### Настройки предотвращения переноса:
- **noWrap** для ячейки (через XML)
- **noBreak** для run (через XML)
- **keep_together = True** для параграфа
- **widow_control = False** для параграфа
- **Очистка параграфа** перед добавлением PIN-кода

---

## 📐 Текущие параметры

### Ширина ячеек:
- **PIN**: 3.0 см (увеличена с 2.0 см)
- **QR**: 5.0 см (не изменилась)

### Отступы параграфов:
- **PIN**: space_before=0, space_after=0, **left_indent=Pt(25)**, right_indent=0
- **QR**: space_before=0, space_after=0, **left_indent=Pt(25)**, right_indent=0

### Выравнивание:
- **PIN-код**: RIGHT (по правому краю) или LEFT (согласно параметру)
- **QR-код**: LEFT (по левому краю) или RIGHT (согласно параметру)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что:
   - PIN-код в одной строке (не переносится)
   - PIN-код и QR-код сдвинуты вправо

---

## ⚠️ Важные замечания

1. **Увеличенная ширина ячейки** (3.0 см) может повлиять на общую ширину таблицы
2. Если PIN-код всё ещё переносится:
   - Увеличьте ширину ячейки до `3.5 см` или `4.0 см`
   - Или уменьшите размер шрифта PIN-кода до `18pt`

3. **Если нужно больше сдвига вправо:**
   - Увеличьте значение `Pt(25)` до `Pt(30)` или `Pt(35)`

4. **Если результат не соответствует ожиданиям:**
   - Проверьте версию LibreOffice/Word
   - Убедитесь, что все настройки для предотвращения переноса применены
   - Проверьте логи для диагностики

---

## 📝 Резюме

**Проблема 1:** PIN-код переносился на две строки  
**Решение 1:** Увеличена ширина ячейки PIN-кода с 2.0 см до 3.0 см  

**Проблема 2:** PIN-код нужно сдвинуть вправо  
**Решение 2:** Подтверждено применение left_indent = Pt(25) для сдвига вправо  

**Применено к:** Ячейки PIN-кода в обеих таблицах  
**Результат:** PIN-код в одной строке, оба элемента сдвинуты вправо ✅

После применения изменений PIN-код будет в одной строке и оба элемента будут сдвинуты вправо!

