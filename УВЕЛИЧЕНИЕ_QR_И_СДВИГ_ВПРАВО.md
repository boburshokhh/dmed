# Увеличение размера QR-кода на 20% и сдвиг PIN и QR вправо

## ✅ Изменения

### 1. Увеличен размер QR-кода на 20%
### 2. Сдвинуты PIN-код и QR-код вместе еще немного вправо

---

## 🔧 Технические изменения

### 1. Увеличен размер QR-кода

**Было:**
```python
qr_width_inches = 1.1  # 1.1 дюйма = ~2.79 см
```

**Стало:**
```python
qr_width_inches = 1.32  # Увеличен на 20% (1.1 * 1.2 = 1.32 дюйма = ~3.35 см)
```

**Расчет:**
- Было: 1.1 дюйма
- Увеличение на 20%: 1.1 × 1.2 = 1.32 дюйма
- В сантиметрах: 1.32 × 2.54 = ~3.35 см

### 2. Увеличен left_indent для обоих элементов

**PIN-код:**
- Было: `Pt(35)` (~35px)
- Стало: `Pt(40)` (~40px, добавлено 5px)

**QR-код:**
- Было: `Pt(10)` (~10px)
- Стало: `Pt(15)` (~15px, добавлено 5px)

**Результат:** Оба элемента сдвинуты вместе на 5px вправо

### 3. Применено в двух местах:
- ✅ Обычная таблица (функция `create_pin_qr_table`)
- ✅ Вложенная таблица (функция `replace_qr_placeholder`)

---

## 📊 Визуальное представление

### До (QR-код меньше, меньше сдвиг):
```
┌─────────────────────────────────────┐
│            6758[QR]                 │  ← QR-код 1.1 дюйма, сдвиг 35px/10px
└─────────────────────────────────────┘
```

### После (QR-код больше на 20%, больше сдвиг):
```
┌─────────────────────────────────────┐
│             6758[QR]                │  ← QR-код 1.32 дюйма (+20%), сдвиг 40px/15px
└─────────────────────────────────────┘
```

---

## 🎯 Результат

После изменений:

✅ **QR-код увеличен на 20%** - с 1.1 до 1.32 дюйма (~3.35 см)  
✅ **PIN-код сдвинут вправо** на ~40px (было 35px, добавлено 5px)  
✅ **QR-код сдвинут вправо** на ~15px (было 10px, добавлено 5px)  
✅ **Оба элемента сдвинуты вместе** (одинаковый дополнительный сдвиг 5px)  
✅ **Относительное расстояние сохранено** (разница между ними не изменилась)

---

## 📐 Технические детали

### Размер QR-кода:
- **Было**: 1.1 дюйма = ~2.79 см
- **Стало**: 1.32 дюйма = ~3.35 см
- **Увеличение**: +20% (0.22 дюйма = ~0.56 см)

### Отступы параграфов:
- **PIN**: left_indent = Pt(40) (~40px, было 35px, добавлено 5px)
- **QR**: left_indent = Pt(15) (~15px, было 10px, добавлено 5px)

### Разница между элементами:
- Разница left_indent: Pt(40) - Pt(15) = Pt(25) (не изменилась)
- Оба элемента сдвинуты на одинаковое значение (5px)

### Проверка размера QR-кода в ячейке:
- Ширина ячейки QR: 5.0 см
- Размер QR-кода: 1.32 дюйма = ~3.35 см
- Left_indent: Pt(15) ≈ 0.4 см
- Запас: 5.0 - 3.35 - 0.4 = ~1.25 см ✅ (достаточно)

---

## 🔄 Что нужно сделать

1. ✅ Код исправлен
2. ⏳ Загрузите обновлённый `document_generator.py` на сервер
3. ⏳ Перезапустите приложение:
   ```bash
   sudo systemctl restart dmed
   ```
4. ⏳ Создайте тестовый документ
5. ⏳ Проверьте, что:
   - QR-код увеличен на 20%
   - PIN-код и QR-код сдвинуты вместе еще немного вправо

---

## ⚠️ Важные замечания

1. **QR-код увеличен на 20%** - теперь 1.32 дюйма вместо 1.1 дюйма
2. **Оба элемента сдвинуты вместе** - одинаковый дополнительный сдвиг 5px
3. **QR-код помещается в ячейку** - ширина ячейки 5.0 см достаточна для QR-кода 3.35 см

4. **Если QR-код обрезается:**
   - Увеличьте ширину ячейки QR-кода до `5.5 см` или `6.0 см`
   - Или уменьшите размер QR-кода до `1.2 дюйма` (увеличение на 10% вместо 20%)

5. **Если нужно еще больше сдвига:**
   - Увеличьте left_indent для обоих элементов еще на 5px

---

## 📝 Резюме

**Изменение 1:** Увеличен размер QR-кода на 20% (с 1.1 до 1.32 дюйма)  
**Изменение 2:** Сдвинуты PIN-код и QR-код вместе на 5px вправо  

**Применено к:** Ячейки PIN-кода и QR-кода в обеих таблицах  
**Результат:** QR-код увеличен на 20%, оба элемента сдвинуты вместе вправо ✅

После применения изменений QR-код будет увеличен на 20%, а PIN-код и QR-код будут сдвинуты вместе еще немного вправо!

